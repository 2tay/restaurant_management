/// Non-translatable constants only.
///
/// User-facing text does **not** belong here — it lives in `lib/l10n/app_fr.arb`
/// and is reached through `AppLocalizations.of(context)`. This file is for
/// values that stay identical in every language: route names, asset paths, and
/// debug-only labels.
abstract final class AppStrings {
  // Route names are filled in during Stage 3 (go_router).
}
